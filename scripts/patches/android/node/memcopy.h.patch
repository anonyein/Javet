--- a/deps/v8/src/utils/memcopy.h
+++ b/deps/v8/src/utils/memcopy.h
@@ -352,7 +352,7 @@ inline void Relaxed_Memset(T* destination, T value, size_t count)
   requires std::is_integral_v<T>
 {
   for (size_t i = 0; i < count; i++) {
-    std::atomic_ref<T>(destination[i]).store(value, std::memory_order_relaxed);
+    destination[i] = value;
   }
 }
 
